type BalanceProps = {
    ratio?: number;
    enabled?: boolean;
};
export declare function balancer(node: HTMLElement, { ratio, enabled }?: BalanceProps): {
    update({ ratio, enabled }: BalanceProps): void;
    destroy(): void;
};
export {};
